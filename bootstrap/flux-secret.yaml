---
# This is needed only for the sync high water mark, and not for a
# deploy key, since I'm using a public, git repository in readonly
# mode.
apiVersion: v1
kind: Secret
metadata:
  name: flux-git-deploy
  namespace: bootstrap
type: Opaque
